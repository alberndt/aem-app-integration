package com.alexanderberndt.aemappintegration.core;

import com.alexanderberndt.aemappintegration.api.IntegrationContext;
import com.alexanderberndt.aemappintegration.api.PlainTextValidator;
import com.alexanderberndt.aemappintegration.api.Validator;
import com.alexanderberndt.aemappintegration.core.impl.RegexValidator;
import org.junit.Test;

import java.lang.reflect.InvocationTargetException;
import java.lang.reflect.Method;

import static org.junit.Assert.*;


public class IntegrationPipelineBuilderTest {


    @Test
    public void someTest() throws NoSuchMethodException, InvocationTargetException, IllegalAccessException {
//
//        PlainTextValidator validator = (PlainTextValidator) new RegexValidator();
//        Object config = validator.convertConfig(null, null);
//
////        for (Method m : PlainTextValidator.class.getMethods()) {
////            System.out.println(m);
////        }
//
//        Method method = PlainTextValidator.class.getMethod("validate", Object.class, Object.class, IntegrationContext.class);
//        method.invoke(validator, "Some string data", config, null);
//
//        System.out.println(method);
//        validator.validate(null, config, null);

    }
}